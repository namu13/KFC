doctype html
html
  head
    include ../partials/head.pug
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js")
    script(src="../js/modal.js")
    meta(charset="utf-8")
    title 책 추가하기
  body
    .add
      .wrap.header
        .header-title 책 추가하기
        div
          button(class="button", onclick="addBook()")
            | 추가하기
      .wrap.container
        div(id="search_book")
          button(class="open_modal button", data-modal="find_book")
            | 책 검색하기
        div(class="add__form")
          img(class="add__form-image", src="", alt="")
          div(class="add__form-inputs")
            table(class="data__inputs")
              tr
                td
                  label(for="book_title") 도서명
                td
                  input(type="text", id="book_title")
              tr
                td
                  label(for="book_writer") 지은이
                td
                  input(type="text", id="book_writer")
              tr
                td
                  label(for="book_publisher") 출판사
                td
                  input(type="text", id="book_publisher")
        label(for="book_experience") 감상평
        textarea(id="book_experience", class="book_experience")
    // The find_book_modal
    .modal(id="find_book_modal")
      .modal-content
        .modal-title 책 검색하기
        .search
          input(class="search__input")
          button(class="search__btn", onclick="getBooks()")
            i.fa-solid.fa-magnifying-glass
        ul(class="search_result")
        div(class="data__inputs-buttons")
          button.button(id="add_library", type="button") 추가
          button(class="button button-cancel close_close", data-modal="find_book") 취소
